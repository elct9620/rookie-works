/ TODO: Add helper to switch between home/project
.col-md-4
  .card.card-blog
    = link_to project, class: 'header' do
      = image_tag project.thumbnail_url(:thumb), class: 'image-header'
    .content
      / .circle-black
      /   .circle
      /     .date-wrapper
      /       / TODO: Add category field
      /       span.month GAME
      /       span.date = project.exhibition_year
      = link_to project, class: 'card-title'
        h3 = project.name
      h6.card-category.text-warning = "#{project.school.name} #{project.department.name}"
      / p.card-description.text-gray = truncate(project.description, length: 100)
      - if params[:controller] == 'projects'
        .line-divider.line-danger
        = link_to t('.edit'), edit_game_path(project.result), class: 'btn btn-small btn-info btn-fill'
        | &nbsp;
        = link_to t('.destroy'), game_path(project.result), class: 'btn btn-small btn-danger btn-fill', method: :delete
